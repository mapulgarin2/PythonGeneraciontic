from Funcion_calcular_area_rectangulo import calcular_area_rectangulo,area_Circulo

base = 12
altura = 3

calcular_A_rectangulo = calcular_area_rectangulo(base,altura)
print(f"El area del rectangulo de base {base}cm y altura {altura}cm es {calcular_A_rectangulo}cm^2")

radio = 2

calcular_radio = area_Circulo(radio)
print(f"El area de un circulo de radio {2}cm es igual a {calcular_radio}cm^2")